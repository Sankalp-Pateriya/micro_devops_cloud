server:
  port: 8079

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: ${GITHUB_URL:https://github.com/batchlcwd/easy_buy_config}
          username: ${GITHUB_USERNAME:batchlcwd}
          password: ${GITHUB_PASSWORD}
          defaultLabel: main




management:
  endpoints:
    web:
      exposure:
        include: health,info



encrypt:
  key: 2525weafasfsadfasdrfsafsadfsadfgsadfsadfsavsadfasdfaws


eureka:
  instance:
    preferIpAddress: true
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka}
